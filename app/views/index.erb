<div>
  <article>
    <% @questions.each do |question| %>
    <form method="post" action='/'>
      <button type="submit" name="submit"></button>
    </form>
      <h2><a href="/questions/<%= question.id %>"><%= question.question_text %></a></h2>
      <ul>
        <li>by: <%= question.poster.username %></li>

      </ul>
    <% end %>
  </article>
</div>
<div>
  <button id="new_question_button" class="new-question-button button center">New Question</button>
        <form id="new_question_form" class="center hidden">
          <label for="question_text">Comment:<br></label>
          <textarea id="question_text" name="question_text"></textarea><br>
          <input type="submit" class="button" value="Submit Question"></input>
        </form>
</div>
